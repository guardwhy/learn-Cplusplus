#include <stdio.h>
/**
 * 变量的定义
 * @return
 */
int main() {
    // 定义整型变量
    int vx, vy;
    // 赋值操作
    vx = 36;
    vy = 36 + 18;

    // 输出结果
    printf("vx的值是: %d\n", vx);
    printf("vy的值是: %d\n", vy);
    return 0;
}