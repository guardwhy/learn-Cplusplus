#include <stdio.h>

int main()
{
    // 定义整形变量
    int a = 10;
    // 定义字符变量
    char b = '@';
    // 定义浮点型变量
    float c = 99.89;

    // 输出结果
    printf("a=%d, b=%c, c=%f\n", a, b, c);

    return 0;
}
